//! Reserved for a Rust end-to-end acceptance test across the three legs.
//!
//! EMPTY AS IT STANDS. The only acceptance test that exists today is
//! `contracts/test/acceptance.mjs`, which drives the Solidity side against the
//! fixtures `mc-return` emits; nothing in this crate runs. It is a workspace
//! member with dependencies on all four crates and no code, so read its
//! presence as an intention rather than as coverage.
